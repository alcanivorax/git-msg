git-msg is a Git-native CLI that generates clear, conventional commit messages directly from your staged changes — and commits only after your confirmation.
It fits naturally into your Git workflow and makes good commit messages the default, not an afterthought
